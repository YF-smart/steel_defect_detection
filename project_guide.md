# 钢材表面缺陷检测系统 —— 项目知识详解

> 从零理解目标检测项目的每个知识点，面试能讲清楚为什么

---

## 一、项目概述

### 做了什么？

在钢材生产线上，用摄像头拍摄钢表面照片 → 系统自动检测并定位 **6 类缺陷**（裂纹、夹杂、斑块、麻点、氧化皮、划痕）→ 输出每个缺陷的类别、位置和置信度。

### 和项目一（叶片病害分类）的区别

| | 项目一：叶片分类 | 项目二：缺陷检测 |
|------|------|------|
| 任务类型 | 图像分类 | 目标检测 |
| 输出 | "这是玉米锈病" | "坐标(89,77,120,118)处有一处裂纹" |
| 模型 | ResNet18 | YOLOv8n |
| 难度 | ⭐⭐ | ⭐⭐⭐ |
| 核心指标 | Accuracy | mAP (mean Average Precision) |

---

## 二、数据集 NEU-DET

### 2.1 数据概况

- 来源：东北大学发布的钢材表面缺陷数据集
- 图片：1800张灰度图 (200×200)
- 类别：6类，每类约300张，**类别非常均衡**
- 标注：VOC XML格式，每张图 1 个缺陷框

### 2.2 六类缺陷

| 中文 | 英文 | 外观特征 | 产线原因 |
|------|------|------|------|
| 裂纹 | crazing | 网状微裂纹 | 热处理应力 |
| 夹杂 | inclusion | 深色不规则块 | 非金属杂质 |
| 斑块 | patches | 浅色斑块 | 表面氧化 |
| 麻点 | pitted_surface | 密集小凹坑 | 轧辊磨损 |
| 氧化皮 | rolled-in_scale | 深色片状 | 氧化铁皮轧入 |
| 划痕 | scratches | 细长线状 | 机械划伤 |

### 2.3 数据特点（面试要讲）

**优点**：类别均衡（每类约300张），不用担心不均衡问题

**挑战**：
1. **灰度图**：ImageNet 预训练权重在 RGB 上训练的，需要灰度→三通道转换
2. **分辨率低**：200×200，缺陷细节有限，太小的缺陷可能检测不到
3. **每图只有一个缺陷**：实际场景一张钢板上可能有多处缺陷
4. **数据量小**：1800张做目标检测算小数据集

---

## 三、核心技术知识点

### 3.1 目标检测 vs 图像分类

**图像分类**：判断图片是什么 → 输出一个类别标签
**目标检测**：判断图片中有什么、在哪里 → 输出多个 (类别+坐标+置信度)

```
分类: [0.92, 0.03, 0.05] → "这是裂纹"
检测: [(裂纹, (2,2,193,194), 0.95), (划痕, (50,80,150,90), 0.88)]
```

### 3.2 YOLO 检测原理

YOLO = You Only Look Once，核心思想是**把检测问题转化为回归问题**：

1. 把图片分成 S×S 的网格
2. 每个网格预测 B 个边界框（中心坐标、宽高）+ 置信度 + 类别概率
3. 一次前向传播同时完成定位和分类

**为什么叫"You Only Look Once"**：早期的检测方法分两步（先找候选区域→再分类），YOLO 一步完成，快了一个数量级。

### 3.3 VOC vs YOLO 标注格式

**VOC XML** (NEU-DET原始格式)：
```
<xmin>2</xmin><ymin>2</ymin><xmax>193</xmax><ymax>194</ymax>
→ 绝对像素坐标，依赖原始图片尺寸
```

**YOLO txt** (训练需要)：
```
0 0.4875 0.4900 0.9550 0.9600
→ 归一化坐标 (cx, cy, w, h)，与图片尺寸无关
```

**转换公式**：
```
cx = (xmin + xmax) / 2 / img_width
cy = (ymin + ymax) / 2 / img_height
w  = (xmax - xmin) / img_width
h  = (ymax - ymin) / img_height
```

### 3.4 灰度图 → 三通道转换

NEU-DET 图片是单通道灰度图 (200×200×1)，YOLOv8 预训练模型期望 RGB 三通道输入。

**方案A（本项目采用）**：`cv2.COLOR_GRAY2BGR` 复制灰度值为 R=G=B，保留所有纹理信息
**方案B**：改模型第一层接受单通道输入，但预训练权重浪费
**方案C**：从零训练（不用预训练），1800张图效果很差

**为什么方案A有效**：YOLOv8 预训练权重的底层卷积核学的是边缘/纹理特征，这些对灰度图同样适用。复制成三通道只是让通道数匹配，不改变图像内容。

### 3.5 mAP 评估指标

目标检测不用 Accuracy，用 **mAP (mean Average Precision)**：

| 指标 | 含义 |
|------|------|
| AP (Average Precision) | 某一类在不同召回率下的平均精确率 |
| mAP@50 | IoU≥0.5 就算检测正确，所有类 AP 的平均 |
| mAP@50-95 | IoU 从 0.5 到 0.95 (步长0.05) 各算一次 mAP 再平均 |

**mAP@50 是工业中最常用的指标**。mAP@50-95 更严格（要求定位更准），数值通常比 mAP@50 低很多。

### 3.6 IoU (交并比)

判断检测框是否"命中"的标准：
```
IoU = 检测框 ∩ 真实框 / 检测框 ∪ 真实框
```
IoU=1 表示完全重合，IoU=0 表示完全不相关。一般 IoU≥0.5 算检测正确。

---

## 四、数据增强策略

小数据集的目标检测需要更强的数据增强：

| 增强 | 作用 | YOLOv8 参数 |
|------|------|------|
| Mosaic | 4张图拼成1张，增加小目标 | mosaic=1.0 |
| MixUp | 两张图按比例混合 | mixup=0.1 |
| HSV 抖动 | 模拟不同光照 | hsv_h/s/v |
| 随机翻转/缩放 | 增加位置多样性 | 自动开启 |

**面试可讲**：Mosaic 增强对工业缺陷检测特别有用——实际产线上缺陷可能出现在钢板的任何位置、任何方向，Mosaic 让模型学会在各种背景下检测缺陷。

---

## 五、面试话术

### 自我介绍

> 我做了个**钢材表面缺陷检测系统**，基于 YOLOv8，能检测 6 类常见钢材缺陷。项目涵盖了从数据预处理（XML→YOLO格式转换、灰度图处理），到模型训练、mAP 评估和推理的完整流程。

### 被问"最难的地方"

> 两个难点：一是数据预处理——NEU-DET 原始标注是 VOC XML 格式，需要转换为 YOLO 的归一化坐标格式，同时灰度图需要转三通道适配预训练模型。二是数据量小（1800张）——我用 Mosaic 和 MixUp 增强策略来防止过拟合，效果提升明显。

### 被问"mAP 是什么"

> mAP 是目标检测的核心评估指标。先算每个类别的 AP（不同召回率下的平均精确率），再对所有类别取平均。mAP@50 表示检测框和真实框 IoU≥50% 就算命中，是工业界最常用的标准。

---

> 训练完成后把实际 mAP 数值填入，面试时直接亮数据
